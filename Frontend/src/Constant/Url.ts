
const BaseUrl: string = 'http://localhost:8000/api/v1';
const SignupUrl: string = `${BaseUrl}/auth/users`;
const LoginUrl: string = `${BaseUrl}/auth/users`;

export default BaseUrl;
export { SignupUrl, LoginUrl };